# Python Data Analysis Pipeline Dependencies
#
# The pipeline uses sentence-transformers (all-MiniLM-L6-v2) for
# semantic embeddings and networkx is used as an optional graph lib.
# rapidfuzz / scikit-learn / pandas / requests are kept for the legacy
# api/ FastAPI prototype and may be removed once that code is deleted.

numpy>=1.24.0
sentence-transformers>=2.2.0
networkx>=3.1

# Optional / used only by the legacy api/ FastAPI prototype
rapidfuzz>=3.6.0
scikit-learn>=1.3.0
pandas>=2.0.0
requests>=2.31.0
